# このリポジトリでは改行コードの自動変換を一切行わない。
#
# 理由: core.autocrlf=true の環境では、KiCad が LF で書いたファイルが
# チェックアウト時に CRLF へ変換され、全行が差分として現れてしまう。
# KiCad の .kicad_sch / .kicad_pcb はツール生成物なので、
# バイト単位でそのまま保存・復元するのが正しい。
* -text
